# fieldmark-updates

Public update manifest for Fieldmark. Not source code — this repo exists only so a deployed
Fieldmark instance can check for a newer version without needing any GitHub credentials (the
main source repo is private).

`latest.json` is published automatically by the main repo's release workflow on every tagged
release. Don't edit it by hand.
